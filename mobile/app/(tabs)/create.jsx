// Placeholder route for the raised ＋ tab slot. The tab button never
// navigates here (it pushes /add, the sheet, instead) — this exists only so
// expo-router registers the slot.
export default function AddTabPlaceholder() {
  return null;
}
