// Public surface (feature-module.md §exports): the two screens app/ routes
// mount, plus RecipeCard — the grid tile cookbook/profile-style grids reuse.
// Everything else (queries, the mealdb transform seam, scaling glue) stays
// feature-private.
export { DiscoverScreen } from './DiscoverScreen';
export { RecipeDetailScreen } from './RecipeDetailScreen';
export { RecipeCard } from './RecipeCard';
