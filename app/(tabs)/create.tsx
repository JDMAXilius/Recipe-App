// The raised ＋ (create) tab IS Chat with Otto — the primary creation path
// (founder decision): describe a dish, Otto writes it inline, you save. Bringing
// in an existing recipe (link/photo/text/write-it-myself) is one tap away via the
// header import door → /add. Manual editor stays reachable there.
export { ChatScreen as default } from '@/features/chat';
